export const metadata = {
  title: 'Servicios — Trendicore',
  description: 'Descubre nuestros servicios: selección de tendencias Gen Z, tracking de precios y alcance global en moda y tech.',
};

export default function ServiciosLayout({ children }) {
  return children;
}
