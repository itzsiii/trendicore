export const metadata = {
  title: 'Entretenimiento — Trendicore',
  description: 'Tendencias virales, lifestyle y cultura visual Gen Z. Recomendaciones de cine, música y series.',
};

export default function EntretenimientoLayout({ children }) {
  return children;
}
