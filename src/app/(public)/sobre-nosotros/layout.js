export const metadata = {
  title: 'Sobre Nosotros — Trendicore',
  description: 'Conoce la misión y visión de Trendicore: conectar a la Gen Z con productos que definen su estilo.',
};

export default function SobreNosotrosLayout({ children }) {
  return children;
}
