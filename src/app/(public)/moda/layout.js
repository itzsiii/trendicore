export const metadata = {
  title: 'Moda Gen Z — Trendicore',
  description: 'Explora moda aesthetic, streetwear y coquette para la Gen Z. Las tendencias más virales de TikTok.',
};

export default function ModaLayout({ children }) {
  return children;
}
